# 실측 — 성격 유전 논문 전반부 1차 렌더 (2026-09-11)

대상(운영자 재지정): 논문 `372055db` "Robust inference and correlates from genetic associations with
personality". 운영자 계획: **전반부만 렌더해 1차 확인 → 이상 없으면 후반부 → 합쳐서 유튜브 업로드.**

## 무엇을 렌더했나
- 원본 지시서 `9b4558cf`(12컷 · 88초, 3D 그래픽). 재생성본 `cc5e8a1a` 는 전반부 차단이 4건으로
  **늘어** 쓰지 않았다(운영자 결정).
- 시퀀스 경계에서 반으로: 전반 컷 1~7(SEQ1 유전체 발견 + SEQ2 뇌 기전), 후반 8~12.
- 전반부 부분 지시서 `37fc17bd` · 렌더 잡 `a688d355`. 남은 차단 1건
  (`vseq_state_lineage_mismatch` 컷 7 — 앞에서 안 나온 빛줄기가 "흐려진다")은 **운영자 결정으로
  강제 통과**, `header.forced_approval` 에 흔적.
- 결과: **46.3초 · $3.94**(이미지 7장 $0.84 + Veo 9클립 62초 $3.10). 판정 `done`.

## 확인된 것
- **화풍: 좋다.** 무광 CG 실험실(REALITY)과 DNA·뇌 모형(MECHANISM)이 재질·조명을 공유해 한
  작품으로 보인다(2026-09-08 확정 화풍 그대로).
- 생성 원본은 제 비율이다(이미지 768×1376, Veo 720×1280). 화면 위아래 검은 띠는 **설계**다
  (`config.LAYOUT_MODE="center_band"` — 콘텐츠 밴드 + 상단 훅·하단 자막 바).
- 클립은 첫↔끝 프레임에 **실제 변화가 있다**(DNA 에 표지가 켜짐, 실험실 인물 늘어남, 뇌로
  다가가며 발광). 다만 느리다 — 아래 미해결 ②.

## 렌더로 찾아 고친 것
| # | 문제 | 고침 |
|---|---|---|
| 1 | 로컬 `.env` 에 `IMAGE_PROVIDER`·`VIDEO_PROVIDER` 가 없으면 기본값 `placeholder` 로 **가짜 그림 영상이 조립되고 잡이 done** 으로 끝났다(첫 시도 `dee3dcfc`, $0) | 그 잡·지시서를 failed 로 무효 처리. 엔진 가드는 별도 작업으로 분리(`process_job` 이 placeholder 면 거부) |
| 2 | 연속성 판정(`continuity_qa.ask`)이 gemini-2.5-flash 에 상한 200 토큰으로 사고를 켠 채 호출 → 본문이 비어 **판정 5회 전부 "판정 불가"** | `llm._gemini_create` 와 같은 규칙: flash 는 사고 끔, pro 는 예산 확대. 실호출로 판정 복구 확인(`same_world: True` + 사유) |
| 3 | stage 영상 원장 9행($3.10)이 `render_job_id`·`directive_id` **없이** 기록 → 잡 기준 조회에서 영상비 0 | `_render_cut_clips` 가 `_build_stage_video` 에 번호를 넘기게. 이번 9행은 SQL 로 잡·지시서에 귀속 |

CLAUDE.md 가 "이미지·영상 원장 경로는 렌더 0회라 미검증"이라 적어 둔 자리가 이번에 처음
실측됐다 — 이미지 원장은 정상, 영상 원장은 위 3번 결함이 있었다.

## 미해결 (후반부 렌더 전에 운영자 판단)
- **② 움직임이 느리다.** 렌더 QA: 영상 컷 7/7 이 "거의 정지"(중앙 움직임 0.0008, 발행 벤치마크
  0.0117). 첫↔끝 변화는 있으니 "멈춤"이 아니라 **느린 카메라**다. 지시서 단계에서 연출 계약 미이행
  으로 전 컷이 invest → standard 로 **강등**돼 후보 2발·카메라 계약이 안 붙었다(그래서 클립은
  stage 당 1개). 원인은 지시서 쪽이다.
- 1.1초 무음 1곳(경고).
- 대시보드: 논문 화면 3D 그래픽 칸은 가장 최근 지시서(부분 지시서 `37fc17bd`)를 보여준다.
