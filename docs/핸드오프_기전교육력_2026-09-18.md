# 핸드오프 — 기전 시퀀스 교육력 T1·T2·T3·T5 구현 (2026-09-18)

> 연구: `docs/연구_기전시퀀스_교육력_2026-09-17.md` (PR #24).
> 운영자 판정: "원리 설명하는 시퀀스가 너무 별로야 … 의미없는 배경사진으로 채워져 있을 뿐이야."
> 운영자 승인: **"T5 색까지 승인"** — T1·T2·T3·T5 구현, T4(문법 카탈로그)는 미선택.

## 1. 무엇을 바꿨나 (파일 기준)

| 처방 | 자리 | 요지 |
|---|---|---|
| T1-a 구조 배선 | `engine/visual_sequence.py::mechanism_prose` → `engine/providers/image.py::_build_image_prompt` | `mechanism`(subject/components/before/after/highlight)이 **이미지 프롬프트에 실린다**. 영어 필드만. 참조 컷은 변화·강조만. `config.IMAGE_PROMPT_CARRIES_MECHANISM` |
| T1-b 분리 게이트 | `engine/photo_contract.py` `photo_mechanism_prompt_detached` (**차단**) | components 가 visual_prompt 에 하나도 안 나오거나 영어 구성요소 < 2. 실측 117컷 중 4컷(3.4%)·오탐 0 → 처음부터 차단. 처방·웹 라벨 동봉 |
| T1-c 여는 컷 배우 | (추가 안 함) | 구조 검사로 91 시퀀스 실측 **0건**. 프롬프트 쪽은 기존 `photo_lead_cut_missing_entity`(2026-09-10)가 이미 덮는다 |
| T2 전·후 분할 스틸 | `engine/render.py` `split_before_after_applies` / `_compose_split_still` / `_build_split_stage_video`, `engine/assemble.py::build_still_video_command` | 상태가 **바뀌는** stage(TRANSFORM·GROW·SHRINK·SPLIT_OFF·MERGE_INTO·DISAPPEAR, `continuity_from` 있음)는 I2V 대신 **앞 stage 그림(위) + 이 stage 그림(아래)** 한 장 + 켄번스. 영상비 0. 캐시 키에 마커. 결정은 `seq_decision["split_before_after"]` |
| T3 범례·캡션 | `engine/config.py` OVERLAY_TYPES, `engine/evidence_overlay.py`, `engine/subtitles.py` | 오버레이 유형 `legend`(■ 색 견본 + 한글 낱말, 좌하단) · `label_pair`(위/아래 캡션, 분할선 기준). ASS 스타일 Legend/LabelTop/LabelBottom. 경고 `photo_mechanism_unlabeled`(RETRYABLE) |
| T5 색 규약 | `engine/config.py` `MECHANISM_COLOR_CODE` / `LEGEND_COLORS_ASS` / `VISUAL_ROLE_STYLE["MECHANISM"]` | amber=설명하는 부분, blue=첫 집단·전, coral=둘째 집단·후. 프롬프트·범례·지시서 안내가 **같은 표**를 읽는다. `tests/test_photo_style_is_locked.py` 를 의도적으로 갱신 |
| 지시서 프롬프트 | `engine/directive.py` | mechanism 필드 **영어** 요구 + `mechanism_ko`(한글 한 줄) 신설, legend/label_pair payload 스키마, 색 규약 안내, "전후 → before_after"(미구현 유형)를 가리키던 안내를 legend/label_pair 로 정정 |
| 기록 | `engine/render.py` `asset_meta["prompt"]` | 실제로 보낸 이미지 프롬프트를 `render_assets.meta` 에 남긴다(연구 때 없어서 못 봤던 것) |
| 웹 | `web/lib/blockLabels.ts`, `web/lib/types.ts`, `web/components/SequenceEditor.tsx` | 새 사유 라벨 2개, OverlayType 2개 + payload, "도해가 보여주는 원리"(mechanism_ko) 한 줄 |

테스트: `tests/test_mechanism_teaching.py`(22건) + 기존 fixture 를 영어 구조로 갱신.

## 2. 실측 (코드 판정, 렌더 전)

저장된 photo 지시서 40편·기전 컷 198개에 새 게이트를 돌린 결과는 §4 에 적는다(구현 직후 측정).

## 3. 알아야 할 것

- **옛 지시서는 재생성해야 한다.** 구조 필드가 한글이면 `photo_mechanism_prompt_detached` 로 막힌다 — 의도다(한글 구조는 그림에 못 간다). 되먹임 처방이 "영어로 적어라"를 말한다.
- **T2 는 정지 화면을 만든다.** 상태가 바뀌는 stage 가 전부 스틸이 되므로 기전 시퀀스에 움직임이 줄어든다. 운동(MOVE·ROTATE·IMPACT)은 여전히 I2V. 너무 정적이면 `MECHANISM_SPLIT_OPERATIONS` 를 좁히거나 `MECHANISM_SPLIT_BEFORE_AFTER=false`.
- **분할 스틸의 위/아래가 무엇인지는 label_pair 가 말한다.** 모델이 안 넣으면 경고만 뜨고 화면엔 캡션이 없다(차단 아님 — 새 어휘라 첫 실측 전엔 차단하지 않는 저장소 규칙).
- `EVIDENCE_OVERLAY_ENABLED` 가 꺼져 있으면 legend/label_pair 도 안 나간다(기존 카드와 같은 스위치).
- 화풍: REALITY 문자열은 그대로. MECHANISM 만 "앰버 1색" → "앰버 + 비교용 두 색". 재질·조명·배경 공유는 `test_the_two_roles_still_share_one_visual_language` 가 계속 지킨다.

## 4. 실측 기록

### 4-1. 게이트 히트율 (저장된 photo 지시서 40편 · 기전 컷 198개, 2026-09-18)

- 설계 전 측정(T1-b 문턱 결정용): 구조가 완전하고 재사용이 아닌 기전 컷 140개 중 **영어 구성요소만** 가진
  117컷에서 구성요소 0겹침 = **4컷(3.4%)**, 넷 다 진짜(entity_id·데이터셋명을 구성요소로 적음). 오탐 0 → 차단.
- 한글 구성요소는 105/473(22%) — 구조 필드 중 subject/transformation/state 는 **84% 가 한글**이었다.
  즉 지금까지의 구조는 그림에 실을 수 없는 언어로 적혀 있었다 → 지시서 프롬프트를 영어 요구로 바꿨다.
- 구현 후 전체 재판정: `photo_mechanism_prompt_detached` **12/40편(42컷)** — 대부분 한글 구성요소.
  옛 지시서는 재생성 대상이므로 의도한 결과. `photo_mechanism_unlabeled` 경고 33/40편(새 어휘라 당연).
- T1-c(여는 컷이 배우를 무대에 안 세움): 91 시퀀스 중 **0건** → 게이트를 추가하지 않았다.
- T2 적용 범위: 뒤따르는 stage 중 상태 변화 선언 71개, 그 71개 **전부** `continuity_from` 이 있어 분할 가능.

### 4-2. 재생성·렌더 판정

(아래 단락은 실측 뒤 채운다)
